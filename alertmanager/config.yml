route:
    receiver: 'slack'

receivers:
    - name: 'slack'
      slack_configs:
          - send_resolved: true
            username: 'pablosantos'
            channel: '#channel_name'
            api_url: 'https://hooks.slack.com/services/'